# Copy this file to .env for local development.
# Do not commit real secrets.

# Required: OpenAI API key used by langchaingo OpenAI client
OPENAI_API_KEY=your_openai_api_key_here

# Optional: HTTP server port (defaults to 8080 if unset)
PORT=8080
